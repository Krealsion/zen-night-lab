# substrate.lock — what Zen `shutter-line` (ZNL-04) experienced.
#
# A record, never an input. Nothing parses this file and no build reads it.
# It exists because an installed Loom cannot say which Loom it is.

loom          55fbb15fda5f8994137dc2d6e3b1c914753f3ec7
zengine       none
abi           6
toolchain     gcc 11.4.0 / cmake 3.22.1 / Ubuntu 22.04.5 LTS (WSL2, kernel 6.6.87.2) on Windows 11
loom-config   -DCMAKE_BUILD_TYPE=Debug -DZEN_BUILD_TESTS=OFF -DZEN_BUILD_EXAMPLES=OFF -DZEN_SDL=OFF
created       2026-08-06

# loom     resolved once from remote refs/heads/main at the start of ZNL-04, by
#          two independent paths that agreed (Windows git over ssh in the live
#          Loom checkout, and WSL git over https to the public URL), then held.
#          A fresh clone was detached at this exact SHA in /tmp, outside the Zen
#          workspace; the live Loom checkout was never a build input.
#          This is the FOURTH distinct Loom this era has run on — the era lock
#          (c86717b0), records-committee (d0d8257), ringing-chamber (b084b1c9)
#          and this one. None of the four evidence sets transfers to another.
# zengine  none, and it was never reached for. A telegraph wants messages,
#          offices, a codebook and a clock on the wall; it wants no window, no
#          renderer, no input and no timer.
# abi      read from the installed include/zen/kernel/abi.h. All four locks in
#          this era measure 6, which is exactly why the ABI cannot stand in for
#          the commit: four different Looms, one compatibility class.
