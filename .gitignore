# Build output, anywhere in the tree.
build/

# The pinned substrate snapshots: reproducible from vendor/README.md, not
# deliverables. The Zengine .so artifacts ARE tracked (see below).
marathon/vendor/loom-src/
marathon/vendor/loom-build/
marathon/vendor/loom/
followups/role-authorship/vendor/loom-src/
followups/role-authorship/vendor/loom-build/
followups/role-authorship/vendor/loom/

# The vendored Zengine artifacts are BINARIES copied from a sibling repo's build
# tree. They are tracked deliberately — an experiment that cannot be re-run is
# not evidence — so they are NOT ignored. See marathon/vendor/README.md.
